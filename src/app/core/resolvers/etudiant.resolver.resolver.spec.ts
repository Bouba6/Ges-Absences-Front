import { EtudiantResolver } from './etudiant.resolver.resolver';

describe('EtudiantResolver', () => {
  it('should create an instance', () => {
    expect(new EtudiantResolver()).toBeTruthy();
  });
});
